name: Build iOS dylib

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: arm64
            sdk: iphoneos
          - arch: arm64e
            sdk: iphoneos
          - arch: x86_64
            sdk: iphonesimulator

    steps:
      - uses: actions/checkout@v4

      - name: Install Lua
        run: brew install lua

      - name: Find Lua paths
        id: lua
        run: |
          LUA_PREFIX=$(brew --prefix lua)
          echo "include=$LUA_PREFIX/include"
          echo "lib=$LUA_PREFIX/lib"
          echo "prefix=$LUA_PREFIX"
        shell: bash

      - name: Build sz.dylib
        run: |
          SDK_PATH=$(xcrun --sdk ${{ matrix.sdk }} --show-sdk-path)
          LUA_INC="${{ steps.lua.outputs.prefix }}/include"
          LUA_LIB="${{ steps.lua.outputs.prefix }}/lib"

          xcrun --sdk ${{ matrix.sdk }} clang \
            -arch ${{ matrix.arch }} \
            -shared \
            -o sz-${{ matrix.arch }}.dylib \
            sz.c \
            -sysroot "$SDK_PATH" \
            -F/Library/Developer/CoreFoundation/System/Library/Frameworks \
            -I"$LUA_INC" \
            -L"$LUA_LIB" \
            -llua

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: sz-${{ matrix.arch }}.dylib
          path: sz-${{ matrix.arch }}.dylib